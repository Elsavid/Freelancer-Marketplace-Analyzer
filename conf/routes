# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index
GET    /explore                     controllers.HomeController.explore
GET    /tutorial                    controllers.HomeController.tutorial
GET    /freelancelot                controllers.HomeController.freeLancelot(request: Request)
GET    /listProjects                controllers.HomeController.listProjects(request: Request)
GET    /search                      controllers.HomeController.search(request: Request)



# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
